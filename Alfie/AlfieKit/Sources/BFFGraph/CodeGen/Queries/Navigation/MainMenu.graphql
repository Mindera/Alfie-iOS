query MainMenuQuery(
    $handle: String!
) {
    menu(handle: $handle) {
        handle
        title
        items {
            id
            title
            url
            items {
                id
                title
                url
                items {
                    id
                    title
                    url
                }
            }
        }
    }
}
