fragment SuggestionProductFragment on SuggestionProduct {
      id
      name
      brandName
      media {
        ...MediaFragment
      }
      price {
        ...PriceFragment
      }
      slug
}
